[pytest]
addopts = --verbose --cov-append --cov-report term --cov tinydb --tb=short
testpaths = tests
markers =
    backend: marks tests as backend-specific failures
    mobile: marks tests as mobile-specific failures
    python: marks tests as python version-specific failures
    regression: marks tests as regression/active failure tests
timeout = 300
timeout_method = thread

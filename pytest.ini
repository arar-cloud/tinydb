[pytest]
addopts=-vv --tb=long --strict-markers --cov-append --cov-report term --cov tinydb
markers =
    backend: Backend layer tests
    mobile: Mobile client integration tests
    web: Web client integration tests
    flaky: Tests known to be flaky or transient
    integration: Integration tests for client-backend communication
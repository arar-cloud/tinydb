[pytest]
addopts=--verbose --cov-append --cov-report term --cov tinydb --benchmark-only --benchmark-warmup=auto --benchmark-save=baseline
markers =
    benchmark: mark test as a performance benchmark
    slow: mark test as slow running
[pytest]
addopts=--verbose --cov-append --cov-report term --cov tinydb
markers=
    slow: marks tests as slow (deselect with '-m "not slow"')
    benchmark: marks tests for performance benchmarking
    stress: marks tests for database stress testing
    profile: marks tests to run with profiler enabled
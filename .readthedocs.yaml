version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"

sphinx:
  configuration: docs/conf.py
  fail_on_warning: false

python:
  version: "3.12"
  install:
    - method: pip
      path: .
      extra_requirements:
        - docs

formats: all
